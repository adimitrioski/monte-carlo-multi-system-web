import { MonteCarloRequest } from './monte-carlo-request';

describe('MonteCarloRequest', () => {
  it('should create an instance', () => {
    expect(new MonteCarloRequest()).toBeTruthy();
  });
});
